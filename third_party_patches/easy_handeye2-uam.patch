diff --git a/easy_handeye2/easy_handeye2/handeye_calibration.py b/easy_handeye2/easy_handeye2/handeye_calibration.py
index 1d490be..7705dd9 100644
--- a/easy_handeye2/easy_handeye2/handeye_calibration.py
+++ b/easy_handeye2/easy_handeye2/handeye_calibration.py
@@ -40,6 +40,10 @@ class HandeyeCalibrationParametersProvider:
 
 def load_calibration(name) -> HandeyeCalibration:
     filepath = filepath_for_calibration(name)
+    return load_calibration_from_file(filepath)
+
+
+def load_calibration_from_file(filepath) -> HandeyeCalibration:
     with open(filepath) as f:
         m = yaml.full_load(f.read())
     ret = HandeyeCalibration()
diff --git a/easy_handeye2/easy_handeye2/handeye_publisher.py b/easy_handeye2/easy_handeye2/handeye_publisher.py
index d499b3f..c33b07f 100755
--- a/easy_handeye2/easy_handeye2/handeye_publisher.py
+++ b/easy_handeye2/easy_handeye2/handeye_publisher.py
@@ -5,7 +5,7 @@ from rclpy.executors import ExternalShutdownException
 from rclpy.node import ParameterType, ParameterDescriptor
 import tf2_ros
 import geometry_msgs.msg
-from easy_handeye2.handeye_calibration import load_calibration
+from easy_handeye2.handeye_calibration import load_calibration, load_calibration_from_file
 
 
 class HandeyePublisher(rclpy.node.Node):
@@ -13,11 +13,19 @@ class HandeyePublisher(rclpy.node.Node):
         super().__init__('handeye_publisher')
 
         self.declare_parameter('name', descriptor=ParameterDescriptor(type=ParameterType.PARAMETER_STRING))
-        name = self.get_parameter('name').get_parameter_value().string_value
-
-        self.get_logger().info(f'Loading the calibration with name {name}')
+        self.declare_parameter('calibration_file', descriptor=ParameterDescriptor(type=ParameterType.PARAMETER_STRING))
 
-        self.calibration = load_calibration(name)
+        name = self.get_parameter('name').get_parameter_value().string_value
+        calibration_file = self.get_parameter('calibration_file').get_parameter_value().string_value
+
+        if calibration_file:
+            self.get_logger().info(f'Loading the calibration from file {calibration_file}')
+            self.calibration = load_calibration_from_file(calibration_file)
+        elif name:
+            self.get_logger().info(f'Loading the calibration with name {name}')
+            self.calibration = load_calibration(name)
+        else:
+            raise ValueError("handeye_publisher requires either 'calibration_file' or 'name' parameter to be set.")
         parameters = self.calibration.parameters
 
         if parameters.calibration_type == 'eye_in_hand':
diff --git a/easy_handeye2/launch/calibrate.launch.py b/easy_handeye2/launch/calibrate.launch.py
index 47dedba..d082cb8 100644
--- a/easy_handeye2/launch/calibrate.launch.py
+++ b/easy_handeye2/launch/calibrate.launch.py
@@ -32,7 +32,7 @@ def generate_launch_description():
 
     handeye_rqt_calibrator = Node(package='easy_handeye2', executable='rqt_calibrator.py',
                                   name='handeye_rqt_calibrator',
-                                  # arguments=['--ros-args', '--log-level', 'debug'],
+                                  arguments=['--force-discover'],
                                   parameters=[{
                                       'name': LaunchConfiguration('name'),
                                       'calibration_type': LaunchConfiguration('calibration_type'),
diff --git a/easy_handeye2/launch/calibrate_with_params.launch.py b/easy_handeye2/launch/calibrate_with_params.launch.py
new file mode 100644
index 0000000..25142fc
--- /dev/null
+++ b/easy_handeye2/launch/calibrate_with_params.launch.py
@@ -0,0 +1,81 @@
+"""
+Helper launch file that wraps calibrate.launch.py and provides convenient
+defaults for all required parameters. You can override any of the declared
+arguments on the command line, e.g.:
+
+    ros2 launch easy_handeye2 calibrate_with_params.launch.py \
+        name:=my_session calibration_type:=eye_on_base \
+        tracking_base_frame:=camera_color_optical_frame \
+        tracking_marker_frame:=aruco_marker_frame \
+        robot_base_frame:=base_link \
+        robot_effector_frame:=tool0
+"""
+
+from ament_index_python.packages import get_package_share_directory
+from launch import LaunchDescription
+from launch.actions import DeclareLaunchArgument, IncludeLaunchDescription
+from launch.launch_description_sources import PythonLaunchDescriptionSource
+from launch.substitutions import LaunchConfiguration
+
+
+def generate_launch_description():
+    calibrate_launch_path = get_package_share_directory("easy_handeye2")
+    calibrate_launch = PythonLaunchDescriptionSource(
+        calibrate_launch_path + "/launch/calibrate.launch.py"
+    )
+
+    name_arg = DeclareLaunchArgument(
+        "name",
+        default_value="handeye_calib",
+        description="Unique identifier for this hand-eye calibration session.",
+    )
+    calibration_type_arg = DeclareLaunchArgument(
+        "calibration_type",
+        default_value="eye_on_base",
+        choices=["eye_in_hand", "eye_on_base"],
+        description="Type of calibration to perform.",
+    )
+    tracking_base_frame_arg = DeclareLaunchArgument(
+        "tracking_base_frame",
+        default_value="camera_color_optical_frame",
+        description="Frame id of the tracking system base (e.g. camera optical frame).",
+    )
+    tracking_marker_frame_arg = DeclareLaunchArgument(
+        "tracking_marker_frame",
+        default_value="aruco_marker_66",
+        description="Frame id of the tracked marker.",
+    )
+    robot_base_frame_arg = DeclareLaunchArgument(
+        "robot_base_frame",
+        default_value="base_link",
+        description="Robot base frame used for calibration.",
+    )
+    robot_effector_frame_arg = DeclareLaunchArgument(
+        "robot_effector_frame",
+        default_value="link_3",
+        description="Robot end-effector frame used for calibration.",
+    )
+
+    include_calibrate = IncludeLaunchDescription(
+        calibrate_launch,
+        launch_arguments={
+            "name": LaunchConfiguration("name"),
+            "calibration_type": LaunchConfiguration("calibration_type"),
+            "tracking_base_frame": LaunchConfiguration("tracking_base_frame"),
+            "tracking_marker_frame": LaunchConfiguration("tracking_marker_frame"),
+            "robot_base_frame": LaunchConfiguration("robot_base_frame"),
+            "robot_effector_frame": LaunchConfiguration("robot_effector_frame"),
+        }.items(),
+    )
+
+    return LaunchDescription(
+        [
+            name_arg,
+            calibration_type_arg,
+            tracking_base_frame_arg,
+            tracking_marker_frame_arg,
+            robot_base_frame_arg,
+            robot_effector_frame_arg,
+            include_calibrate,
+        ]
+    )
diff --git a/easy_handeye2/launch/publish.launch.py b/easy_handeye2/launch/publish.launch.py
index c9c6495..965eb85 100644
--- a/easy_handeye2/launch/publish.launch.py
+++ b/easy_handeye2/launch/publish.launch.py
@@ -5,13 +5,20 @@ from launch_ros.actions import Node
 
 
 def generate_launch_description():
-    arg_name = DeclareLaunchArgument('name')
+    arg_name = DeclareLaunchArgument('name', default_value='', description='Saved calibration name to load.')
+    arg_calibration_file = DeclareLaunchArgument(
+        'calibration_file',
+        default_value='',
+        description='Absolute path to a calibration YAML file to load (overrides name if set).'
+    )
 
     handeye_publisher = Node(package='easy_handeye2', executable='handeye_publisher', name='handeye_publisher', parameters=[{
         'name': LaunchConfiguration('name'),
+        'calibration_file': LaunchConfiguration('calibration_file'),
     }])
 
     return LaunchDescription([
         arg_name,
+        arg_calibration_file,
         handeye_publisher,
     ])
diff --git a/easy_handeye2/package.xml b/easy_handeye2/package.xml
index d10d5b1..2b0a4c4 100644
--- a/easy_handeye2/package.xml
+++ b/easy_handeye2/package.xml
@@ -21,7 +21,7 @@
 
     <exec_depend>rclpy</exec_depend>
     <exec_depend>tf2_ros</exec_depend>
-    <exec_depend>python-transforms3d-pip</exec_depend>
+    <exec_depend>python3-transforms3d</exec_depend>
     <exec_depend>python3-opencv</exec_depend>
 
     <exec_depend version_gte="0.2.19">python_qt_binding</exec_depend>
