diff --git a/ros2_aruco/config/aruco_parameters.yaml b/ros2_aruco/config/aruco_parameters.yaml
index f2d4aa9..7432a55 100644
--- a/ros2_aruco/config/aruco_parameters.yaml
+++ b/ros2_aruco/config/aruco_parameters.yaml
@@ -1,6 +1,7 @@
 /aruco_node:
   ros__parameters:
-    marker_size: 0.055
-    aruco_dictionary_id: DICT_5X5_250
-    image_topic: /image_raw
-    camera_info_topic: /camera_info
+    marker_size: 0.1
+    aruco_dictionary_id: DICT_ARUCO_ORIGINAL
+    image_topic: /camera/camera/color/image_raw
+    camera_info_topic: /camera/camera/color/camera_info
+    target_marker_id: 66
diff --git a/ros2_aruco/ros2_aruco/aruco_node.py b/ros2_aruco/ros2_aruco/aruco_node.py
index 2fe85ec..77e2e58 100644
--- a/ros2_aruco/ros2_aruco/aruco_node.py
+++ b/ros2_aruco/ros2_aruco/aruco_node.py
@@ -27,18 +27,67 @@ Version: 10/26/2020
 
 """
 
-import rclpy
 import rclpy.node
+import rclpy
 from rclpy.qos import qos_profile_sensor_data
 from cv_bridge import CvBridge
 import numpy as np
 import cv2
-import tf_transformations
 from sensor_msgs.msg import CameraInfo
 from sensor_msgs.msg import Image
-from geometry_msgs.msg import PoseArray, Pose
+from geometry_msgs.msg import PoseArray, Pose, TransformStamped
 from ros2_aruco_interfaces.msg import ArucoMarkers
 from rcl_interfaces.msg import ParameterDescriptor, ParameterType
+import tf2_ros
+
+
+def quaternion_from_rotation_matrix(rotation):
+    """Return x, y, z, w quaternion from a 3x3 rotation matrix."""
+    trace = np.trace(rotation)
+    if trace > 0.0:
+        scale = np.sqrt(trace + 1.0) * 2.0
+        return np.array(
+            [
+                (rotation[2, 1] - rotation[1, 2]) / scale,
+                (rotation[0, 2] - rotation[2, 0]) / scale,
+                (rotation[1, 0] - rotation[0, 1]) / scale,
+                0.25 * scale,
+            ]
+        )
+
+    axis = int(np.argmax(np.diag(rotation)))
+    if axis == 0:
+        scale = np.sqrt(1.0 + rotation[0, 0] - rotation[1, 1] - rotation[2, 2]) * 2.0
+        quat = np.array(
+            [
+                0.25 * scale,
+                (rotation[0, 1] + rotation[1, 0]) / scale,
+                (rotation[0, 2] + rotation[2, 0]) / scale,
+                (rotation[2, 1] - rotation[1, 2]) / scale,
+            ]
+        )
+    elif axis == 1:
+        scale = np.sqrt(1.0 + rotation[1, 1] - rotation[0, 0] - rotation[2, 2]) * 2.0
+        quat = np.array(
+            [
+                (rotation[0, 1] + rotation[1, 0]) / scale,
+                0.25 * scale,
+                (rotation[1, 2] + rotation[2, 1]) / scale,
+                (rotation[0, 2] - rotation[2, 0]) / scale,
+            ]
+        )
+    else:
+        scale = np.sqrt(1.0 + rotation[2, 2] - rotation[0, 0] - rotation[1, 1]) * 2.0
+        quat = np.array(
+            [
+                (rotation[0, 2] + rotation[2, 0]) / scale,
+                (rotation[1, 2] + rotation[2, 1]) / scale,
+                0.25 * scale,
+                (rotation[1, 0] - rotation[0, 1]) / scale,
+            ]
+        )
+
+    return quat / np.linalg.norm(quat)
 
 
 class ArucoNode(rclpy.node.Node):
@@ -90,6 +139,14 @@ class ArucoNode(rclpy.node.Node):
                 description="Camera optical frame to use.",
             ),
         )
+        self.declare_parameter(
+            name="target_marker_id",
+            value=66,
+            descriptor=ParameterDescriptor(
+                type=ParameterType.PARAMETER_INTEGER,
+                description="If >= 0, only publish detections for this marker id.",
+            ),
+        )
 
         self.marker_size = (
             self.get_parameter("marker_size").get_parameter_value().double_value
@@ -114,6 +171,11 @@ class ArucoNode(rclpy.node.Node):
         self.camera_frame = (
             self.get_parameter("camera_frame").get_parameter_value().string_value
         )
+        self.target_marker_id = (
+            self.get_parameter("target_marker_id").get_parameter_value().integer_value
+        )
+        if self.target_marker_id >= 0:
+            self.get_logger().info(f"Filtering detections to marker id: {self.target_marker_id}")
 
         # Make sure we have a valid dictionary id:
         try:
@@ -145,9 +207,20 @@ class ArucoNode(rclpy.node.Node):
         self.intrinsic_mat = None
         self.distortion = None
 
-        self.aruco_dictionary = cv2.aruco.Dictionary_get(dictionary_id)
-        self.aruco_parameters = cv2.aruco.DetectorParameters_create()
+        self.aruco_dictionary = cv2.aruco.getPredefinedDictionary(dictionary_id)
+        if hasattr(cv2.aruco, "DetectorParameters_create"):
+            self.aruco_parameters = cv2.aruco.DetectorParameters_create()
+        else:
+            self.aruco_parameters = cv2.aruco.DetectorParameters()
+
+        if hasattr(cv2.aruco, "ArucoDetector"):
+            self.aruco_detector = cv2.aruco.ArucoDetector(
+                self.aruco_dictionary, self.aruco_parameters
+            )
+        else:
+            self.aruco_detector = None
         self.bridge = CvBridge()
+        self.tf_broadcaster = tf2_ros.TransformBroadcaster(self)
 
     def info_callback(self, info_msg):
         self.info_msg = info_msg
@@ -174,9 +247,12 @@ class ArucoNode(rclpy.node.Node):
         markers.header.stamp = img_msg.header.stamp
         pose_array.header.stamp = img_msg.header.stamp
 
-        corners, marker_ids, rejected = cv2.aruco.detectMarkers(
-            cv_image, self.aruco_dictionary, parameters=self.aruco_parameters
-        )
+        if self.aruco_detector is not None:
+            corners, marker_ids, rejected = self.aruco_detector.detectMarkers(cv_image)
+        else:
+            corners, marker_ids, rejected = cv2.aruco.detectMarkers(
+                cv_image, self.aruco_dictionary, parameters=self.aruco_parameters
+            )
         if marker_ids is not None:
             if cv2.__version__ > "4.0.0":
                 rvecs, tvecs, _ = cv2.aruco.estimatePoseSingleMarkers(
@@ -187,14 +263,16 @@ class ArucoNode(rclpy.node.Node):
                     corners, self.marker_size, self.intrinsic_mat, self.distortion
                 )
             for i, marker_id in enumerate(marker_ids):
+                marker_value = marker_id[0]
+                if self.target_marker_id >= 0 and marker_value != self.target_marker_id:
+                    continue
                 pose = Pose()
                 pose.position.x = tvecs[i][0][0]
                 pose.position.y = tvecs[i][0][1]
                 pose.position.z = tvecs[i][0][2]
 
-                rot_matrix = np.eye(4)
-                rot_matrix[0:3, 0:3] = cv2.Rodrigues(np.array(rvecs[i][0]))[0]
-                quat = tf_transformations.quaternion_from_matrix(rot_matrix)
+                rot_matrix = cv2.Rodrigues(np.array(rvecs[i][0]))[0]
+                quat = quaternion_from_rotation_matrix(rot_matrix)
 
                 pose.orientation.x = quat[0]
                 pose.orientation.y = quat[1]
@@ -203,7 +281,20 @@ class ArucoNode(rclpy.node.Node):
 
                 pose_array.poses.append(pose)
                 markers.poses.append(pose)
-                markers.marker_ids.append(marker_id[0])
+                markers.marker_ids.append(marker_value)
+
+                transform = TransformStamped()
+                transform.header.stamp = img_msg.header.stamp
+                transform.header.frame_id = pose_array.header.frame_id
+                transform.child_frame_id = f"aruco_marker_{marker_value}"
+                transform.transform.translation.x = tvecs[i][0][0]
+                transform.transform.translation.y = tvecs[i][0][1]
+                transform.transform.translation.z = tvecs[i][0][2]
+                transform.transform.rotation.x = quat[0]
+                transform.transform.rotation.y = quat[1]
+                transform.transform.rotation.z = quat[2]
+                transform.transform.rotation.w = quat[3]
+                self.tf_broadcaster.sendTransform(transform)
 
             self.poses_pub.publish(pose_array)
             self.markers_pub.publish(markers)
